# Единые окончания строк в репозитории независимо от ОС коммитящего.
# В рабочей копии Windows получает CRLF, в репозитории всегда LF.
* text=auto eol=lf

# Бинарные файлы не трогать.
*.bin binary
*.elf binary
*.png binary
*.jpg binary
