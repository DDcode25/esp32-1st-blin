#pragma once

#include "port.h"

// Запускает консоль на UART0 — том же порту, где идёт лог.
//
// Команды позволяют смотреть счётчики, включать дамп трафика и менять
// режимы портов без пересборки. Список — команда help.
void console_start(port_t *port0, port_t *port1);
