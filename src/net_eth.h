#pragma once

#include <Arduino.h>

// Поднимает Ethernet со статическим адресом согласно роли модуля.
// Возвращает управление сразу, не дожидаясь линка — состояние опрашивается
// через ethLinkUp(). Кабель может быть воткнут позже, это не ошибка.
void ethBegin();

// true, если физический линк установлен и адрес назначен.
bool ethLinkUp();

// Вызывать периодически из loop() — обслуживает логирование смены состояния.
void ethLoop();
